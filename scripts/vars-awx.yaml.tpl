---
awx_hostname: awx.${domain}
awx_localname: awx.${domainlocal}
aws_access_key: ${access_key}
aws_secret_key: ${secret_key}
domain: ${domain}
project: ${project}
ec2_type: ${ec2_type}
region: ${region}
ami_id: ${ami_id}
