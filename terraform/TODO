
runcmd: ## convert all of this into ansible-playbook maybe
  - sudo systemctl enable --now docker
  - git clone -b 17.1.0 https://github.com/ansible/awx
  - sudo pip3 install --upgrade pip
  - sudo pip3 install docker-compose
  - >- ansible-playbook -e "awx_task_hostname=awx-task.${project}.${domain}"
                        -e "awx_web_hostname=awx.${project}.${domain}"
                        -i awx/installer/inventory
                        awx/installer/install.yaml

start AWX